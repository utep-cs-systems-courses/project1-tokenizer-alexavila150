# compiler flags. -g for debug, -03 for optimization
CFLAGS=-g

# first target is built by default
all: tokenize

# include file dependencies
tester.o: tester.c
main.o: main.c

# $@=target, $^=prerequisites
tokenize: tester.o main.o
	cc -o $@ $^ $(CFLAGS) 

# deletes files generated by compilation
clean:
	rm -f *.o tokenize

run:tokenize
	./tokenize
